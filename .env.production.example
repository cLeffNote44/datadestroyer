# Production environment configuration (example)
# Copy to .env.production and set real values. Keep this file out of version control.

# Core Django
DJANGO_SETTINGS_MODULE=destroyer.settings.production
DJANGO_SECRET_KEY=REPLACE_WITH_A_LONG_RANDOM_SECRET

# Hosts and CSRF
DJANGO_ALLOWED_HOSTS=yourdomain.com,api.yourdomain.com
DJANGO_CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# HTTPS security
DJANGO_SECURE_HSTS_SECONDS=31536000
DJANGO_SECURE_PROXY_SSL_HEADER=1

# CORS (optional, if you have a separate frontend)
DJANGO_CORS_ALLOWED_ORIGINS=https://yourfrontend.com

# Database (example for PostgreSQL; adjust ENGINE/NAME/USER/PASSWORD/HOST/PORT)
# You can also configure DATABASE_URL and use dj-database-url if preferred.
#DATABASE_URL=postgres://user:password@db:5432/detective
